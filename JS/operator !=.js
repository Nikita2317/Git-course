// Оператор сравнения !=. Возращает значение true, если переменные не равны.
function testNotEqual(val) {
    if (val != 99) {// Функция вернет Not Equal, если val будет иметь любое значение кроме 99.
      return "Not Equal";
    }
    return "Equal";//Функция вернет Equal, если val будет равен 99.
  }
  