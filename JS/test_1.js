// Оператор сравнения ==
function testEqual(val) {
    if (val == 12) { //если val будет равен 12, значит значит условие истино, и функция вернет Equal 
      return "Equal";
    }
    return "Not Equal"; // если услловия не выполняеться функция вернет Not Equal
  }